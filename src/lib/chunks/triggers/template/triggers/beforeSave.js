/**
 * Before save trigger.
 * Another reason to run code in the cloud is to enforce a particular data format.
 * For example, you might have both an Android and an iOS app, and you want to validate data for each of those.
 * Rather than writing code once for each client environment, you can write it just once with Cloud Code.
 * https://docs.servable.app/docs/protocols/api/triggers
 * https://docs.parseplatform.org/cloudcode/guide/#delete-triggers
 * @return {void} Always returns void.
 * @param {Object} request Parse server request.
 */

export default async ({ request }) => {

}
