
/**
* @description
afterFind

Availability and exact behavior depend on your chosen engine's cloud code support - check its
own docs.

In some cases you may want to manipulate the results of a query before they are sent to the client.
You can do so with the afterFind trigger.

https://docs.servable.app/docs/protocols/api/triggers

*/
export default async ({ request }) => {

}
