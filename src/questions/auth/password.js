export default ({
  _clinextType: "question",
  name: 'registryPassword',
  type: 'string',
  promptType: 'password',
  alias: 'u',
  message: 'Password',
  // validators: [{
  //   regex: "^(?=.*[A-Za-z])(?=.*\d)[A-Za-z\d]{8,}$"
  // }]
})
